Work in progress. An experiment to see how much faster Websockets are
compared to jQuery AJAX.

To test, start the two servers:

 $ python sock_app.py --logging=error [--debug]
 $ python app.py --port=8888 --logging=error [--debug]
 
Then visit: http://localhost:8888/ajaxtest and
http://localhost:8888/socktest


  
 
